import 'package:flutter/material.dart';

class WebResponsive extends StatelessWidget {
  const WebResponsive({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This is Web")),
    );
  }
}
